# vecmeitu ballīte (classic snake loop)

Small browser game with classic snake mechanics:
- Grid movement
- Growth on collectible
- Score
- Game over on collision
- Restart and pause

Theme:
- Moving characters: two brides (`👰` + `👰`)
- Collectible: car (`🚗`)

## Run locally

From this folder:

```bash
cd /Users/martakrigere/Documents/MI/Codex/Dators
python3 -m http.server 8000
```

Open:

`http://localhost:8000`

You can also open `index.html` directly.

## Share as a public link (GitHub Pages)

1. Push this folder to a GitHub repository.
2. Open repository `Settings` -> `Pages`.
3. Under `Build and deployment`, set:
   - `Source`: `Deploy from a branch`
   - `Branch`: `main`
   - `Folder`: `/ (root)`
4. Save and wait 1-2 minutes.
5. Share the generated URL:
   - `https://<your-username>.github.io/<repo-name>/`
